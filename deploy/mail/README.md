# Mailpit local

Mailpit captura correo SMTP sintético y lo muestra en una interfaz web local. No
envía mensajes a Internet ni representa la selección de un proveedor
productivo. Su base SQLite vive en el volumen nombrado `mailpit_data`.

La guía operativa está en `docs/development/local-environment.md`.
